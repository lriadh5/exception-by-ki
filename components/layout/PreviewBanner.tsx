export function PreviewBanner() {
  return (
    <div className="bg-paper-dim border-b border-line text-center text-[11px] tracking-[0.08em] uppercase text-ink-soft px-4 py-2">
      Preview build — sample catalog, checkout disabled until a Shopify store is connected
    </div>
  );
}
